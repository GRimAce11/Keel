//
//  __PROJECT_NAME__Tests.swift
//  __PROJECT_NAME__Tests
//

import Testing
@testable import __PROJECT_NAME__

@Suite("__PROJECT_NAME__")
struct __PROJECT_NAME__Tests {

    @Test("The app target is linked and testable")
    func targetIsLinked() {
        // Replace this with your first real test. It exists so the suite is
        // never empty — an empty test target passes silently, which hides the
        // fact that nothing is being checked.
        #expect(Bool(true))
    }
}
