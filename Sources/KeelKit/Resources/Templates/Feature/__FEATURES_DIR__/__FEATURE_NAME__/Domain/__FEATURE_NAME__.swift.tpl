//
//  __FEATURE_NAME__.swift
//  __PROJECT_NAME__
//

import Foundation

/// What the rest of the app means by a __FEATURE_NAME__.
///
/// Nothing in here knows where the data came from. That is what lets the
/// storage behind it change without the screens noticing.
struct __FEATURE_NAME__: Identifiable, Equatable, Sendable {
    let id: Int
    let title: String
}
